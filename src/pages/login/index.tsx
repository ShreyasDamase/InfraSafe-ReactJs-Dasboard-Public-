import React from "react";
import LoginCard from "./components/LoginCard";
export default function index() {
  return (
    <section className="  backgroundImage  h-screen">
      <div className="  h-screen flex justify-end items-center mr-8">
        <LoginCard />
      </div>
    </section>
  );
}
